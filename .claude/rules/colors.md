# Color Rules
Never use raw hex or Tailwind default color classes.
Always use Lumea semantic token classes.
CSS variables are in globals.css — never modify them.
Tailwind mappings are in tailwind.config.ts — never remove entries.